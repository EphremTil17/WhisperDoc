# Shared base — installed by ALL engine images.
# NOTE: torch is NOT here. Whisper needs CPU-only torch (CTranslate2 owns GPU),
# while Parakeet needs CUDA-enabled torch (NeMo uses torch.cuda directly).
# Each engine's requirements file pulls the correct torch variant.

# Framework & IO
fastapi==0.135.1
uvicorn==0.42.0
python-multipart==0.0.22
websockets==16.0
# requests is kept for OIDC stability
requests==2.32.5

# Authentication & Security
python-jose[cryptography]==3.5.0
cachetools==7.0.5

# Utilities & Testing
loguru==0.7.3
uvloop==0.22.1
orjson==3.11.7
pytest==9.0.2
pytest-asyncio==1.3.0
