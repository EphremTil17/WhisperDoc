services:
  whisper-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        - API_PORT=${API_PORT:-9989}
    ports:
      - "${API_PORT:-9989}:${API_PORT:-9989}"
    environment:
      - CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-0}
      - API_PORT=${API_PORT:-9989}
      - API_HOST=${API_HOST:-0.0.0.0}
      - MODEL_NAME=${MODEL_NAME:-medium.en}
      - MODEL_DEVICE=${MODEL_DEVICE:-cuda}
      - MODEL_COMPUTE_TYPE=${MODEL_COMPUTE_TYPE:-float16}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    runtime: nvidia  # Requires nvidia-docker2
    volumes:
      - ./model-cache:/app/model-cache
      - ./assets:/app/assets
    restart: unless-stopped
    env_file:
      - .env

  whisper-test:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-0}
    runtime: nvidia  # Requires nvidia-docker2
    volumes:
      - ./assets:/app/assets
      - ./tests:/app/tests
    command: ["python3", "tests/test_whisper.py"]
    profiles:
      - test
    env_file:
      - .env
